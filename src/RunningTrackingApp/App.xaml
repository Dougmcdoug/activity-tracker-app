<Application x:Class="RunningTrackingApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RunningTrackingApp"
             xmlns:views="clr-namespace:RunningTrackingApp.Views"
             xmlns:vm="clr-namespace:RunningTrackingApp.ViewModels">
    <Application.Resources>
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:GPXImportViewModel}">
            <views:GPXImportView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:GPSTraceViewModel}">
            <views:GPSTraceView/>
        </DataTemplate>
    </Application.Resources>
</Application>

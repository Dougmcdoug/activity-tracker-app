<UserControl x:Class="RunningTrackingApp.Views.GPXImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RunningTrackingApp.Views"
             xmlns:viewmodels="clr-namespace:RunningTrackingApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome to the GPX Import View!" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Button Content="Back to Home" Grid.Row="1" Command="{Binding NavigateToHomeCommand}"/>
    </Grid>
</UserControl>
